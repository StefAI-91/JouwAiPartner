import { StorylinePage } from "./storyline-page";

export const metadata = {
  title: "Theme storyline prototype — Jouw AI Partner",
};

export default function Page() {
  return <StorylinePage />;
}
