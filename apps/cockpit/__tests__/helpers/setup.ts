// Vitest setup — jest-dom matchers (toHaveAttribute, toBeInTheDocument, ...)
// voor jsdom-component-tests. Server-side tests (.ts in node-environment)
// raken dit niet aan.
import "@testing-library/jest-dom/vitest";
