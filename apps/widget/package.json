{
  "name": "@repo/widget",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs && node scripts/generate-health.mjs && node scripts/check-bundle-size.mjs",
    "type-check": "tsc --noEmit",
    "lint": "echo 'no lint configured (esbuild bundle, geen Next.js)'",
    "test": "echo 'no tests yet (V0 scaffold + dummy modal)'"
  },
  "dependencies": {
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "esbuild": "^0.24.0",
    "typescript": "^5"
  }
}
